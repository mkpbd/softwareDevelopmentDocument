# **MongoDB Built-in Objects / Functions / Operators**

I’m grouping them so you understand clearly.





# **2. CRUD Operators**

## **🔸 Insert**

- `insertOne()`
    
- `insertMany()`
    

## **🔸 Read**

- `find()`
    
- `findOne()`
    
- `findOneAndUpdate()`
    
- `findOneAndDelete()`
    
- `countDocuments()`
    
- `estimatedDocumentCount()`
    

## **🔸 Update**

- `updateOne()`
    
- `updateMany()`
    
- `$set`
    
- `$unset`
    
- `$inc`
    
- `$push`
    
- `$pull`
    
- `$addToSet`
    
- `$rename`
    
- `$setOnInsert`
    

## **🔸 Delete**

- `deleteOne()`
    
- `deleteMany()`
    

---

# 📌 **3. Query Operators**

## **🔸 Comparison**

- `$eq`
    
- `$ne`
    
- `$gt`
    
- `$lt`
    
- `$gte`
    
- `$lte`
    
- `$in`
    
- `$nin`
    

## **🔸 Logical**

- `$and`
    
- `$or`
    
- `$nor`
    
- `$not`
    

## **🔸 Element**

- `$exists`
    
- `$type`
    

## **🔸 Evaluation**

- `$regex`
    
- `$expr`
    
- `$function` (custom JS)
    

---

# 📌 **4. Array Operators**

- `$size`
    
- `$all`
    
- `$elemMatch`
    
- `$slice`
    

---

# 📌 **5. Projection Operators**

- `1` (include)
    
- `0` (exclude)
    
- `$slice`
    
- `$meta`
    

---

# 📌 **6. Aggregation Stages**

### **Common pipeline stages:**

- `$match`
    
- `$group`
    
- `$project`
    
- `$sort`
    
- `$limit`
    
- `$skip`
    
- `$lookup` (join)
    
- `$unwind`
    
- `$addFields`
    
- `$set`
    
- `$unset`
    
- `$count`
    
- `$facet`
    
- `$replaceRoot`
    
- `$merge`
    
- `$out`
    

---

# 📌 **7. Aggregation Operators**

### **Group Operators**

- `$sum`
    
- `$avg`
    
- `$min`
    
- `$max`
    
- `$first`
    
- `$last`
    
- `$push`
    
- `$addToSet`
    

### **Arithmetic**

- `$add`
    
- `$subtract`
    
- `$multiply`
    
- `$divide`
    
- `$mod`
    

### **String**

- `$concat`
    
- `$substr`
    
- `$toUpper`
    
- `$toLower`
    
- `$trim`
    
- `$split`
    

### **Array**

- `$map`
    
- `$filter`
    
- `$reduce`
    
- `$size`
    
- `$reverseArray`
    

---

# 📌 **8. Index Functions**

- `createIndex()`
    
- `dropIndex()`
    
- `getIndexes()`
    

Index types:

- Single field index
    
- Compound index
    
- TTL index
    
- Unique index
    
- Partial index
    
- Text index
    
- Geospatial index
    

---

# 📌 **9. Administrative Commands**

- `show dbs`
    
- `show collections`
    
- `use databaseName`
    
- `db.dropDatabase()`
    
- `db.collection.drop()`
    

---

# 📌 **10. MongoDB Shell Built-in Objects**

- `db`
    
- `collection`
    
- `cursor`
    
- `it` (iterate cursor)
    
- `help`
    
- `cls` (clear console)