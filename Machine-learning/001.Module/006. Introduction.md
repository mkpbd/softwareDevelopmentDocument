
6-1 Introduction

## Learning Objectives

এই ভিডিওটির শেষে আপনি —

*   Exploratory Data Analysis (EDA) কী এবং কেন গুরুত্বপূর্ণ তা গভীরভাবে বুঝতে পারবেন।
*   Machine Learning workflow–এর কোন ধাপে EDA আসে এবং কেন model-building এর আগে এটি অপরিহার্য, তা ব্যাখ্যা করতে পারবেন।
*   Dataset বুঝে নেওয়ার জন্য কোন কোন ধরনের প্রশ্ন, পরীক্ষা এবং observation প্রয়োজন হয় তা বিস্তারিতভাবে শিখবেন।
*   Module 06–এ ঠিক কী কী skills অর্জন করবেন এবং প্রতিটি section কী নিয়ে কাজ করবে তার একটি স্পষ্ট roadmap পাবেন।
*   Data-এর quality, structure এবং problem–specific characteristics যাচাই করার প্রাথমিক mindset তৈরি হবে।

## Step-by-Step Explanation

Exploratory Data Analysis (EDA) হলো Machine Learning project–এর foundational step। এই ধাপ ছাড়া modeling শুরু করাটা অনেকটা অন্ধভাবে ভবিষ্যদ্বাণী করার মতো। কারণ EDA–র মূল লক্ষ্য হলো — data–কে বোঝা, এর pattern–গুলো খুঁজে বের করা, সমস্যা শনাক্ত করা এবং model–এর আগে dataset–কে এমনভাবে প্রস্তুত করা যাতে model কার্যকরভাবে শিখতে পারে।

EDA করার সময় একজন Data Scientist সাধারণত dataset–এর নিচের বিষয়গুলো পর্যবেক্ষণ করেন —

*   Dataset–এ total কতটি row ও column আছে (shape)।
*   প্রতিটি feature কী ধরনের data ধারণ করে (data types)।
*   Missing values কোন কোন column–এ আছে এবং সেগুলোর পরিমাণ কত।
*   Numerical feature–গুলোর distribution symmetric নাকি skewed।
*   Categorical feature–গুলোতে imbalance আছে কি না।
*   Target variable-এর সাথে কোন feature-এর logical বা statistical সম্পর্ক থাকতে পারে।

EDA–র মূল উদ্দেশ্য হলো data–কে readable, understandable এবং usable করে তোলা। এর মাধ্যমে আপনি data–র hidden structure, pattern এবং সমস্যাগুলোর একটি clear ছবি পান।

### ⭐ কেন EDA এত গুরুত্বপূর্ণ?

Machine Learning model তখনই ভালো perform করে যখন dataset সঠিকভাবে clean, formatted এবং balanced থাকে। Raw data–তে সাধারণত কিছু সমস্যা থাকেই —

*   Outliers model-এর prediction–এ noise তৈরি করে।
*   Missing values model–কে ভুল pattern শিখায়।
*   Skewed distribution model–কে biased করে তোলে।
*   Unnecessary বা irrelevant feature model–এর complexity বাড়ায় কিন্তু performance কমায়।

EDA–র মাধ্যমে আপনি —

*   Outlier detection করতে পারেন।
*   Missing value–এর ধরন অনুযায়ী Imputation technique পরিকল্পনা করতে পারেন।
*   Distribution দেখে transformation (log/sqrt) দরকার কি না ঠিক করতে পারেন।
*   Feature selection এবং engineering–এর শুরুটা শক্তভাবে করতে পারেন।

EDA data understanding–এর সেতুবন্ধ তৈরি করে, যা ছাড়া model-building পুরোপুরি incomplete থেকে যায়।

## Code Blocks (Notebook থেকে নেওয়া)

> **Note:** এই introductory ভিডিওতে সাধারণত heavy কোড থাকে না, কিন্তু notebook–এ EDA শুরু করার জন্য একটি basic setup দেওয়া থাকে। সেই সেটআপ অপরিবর্তিত অবস্থায় নিচে যুক্ত হলো।

```c
# Basic Imports for EDA Module
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

# Settings
df = pd.read_csv('your_dataset.csv')
df.head()
```

এখানে ব্যবহৃত libraries—

*   **pandas**: data manipulation
*   **numpy**: numerical operations
*   **matplotlib** এবং **seaborn**: visualization

Notebook–এ যদি অতিরিক্ত initialization, style settings বা dataset loading থাকে, পরবর্তী lesson–এ সেগুলো বিশদভাবে ব্যাখ্যা করা হবে।

## Outputs / Charts Explanation

উপরের কোড রান করলে সাধারণত প্রথম যে আউটপুটটি পাওয়া যায় তা হলো — `df.head()`। এটি dataset–এর প্রথম পাঁচটি row দেখায়, যা আপনাকে data–এর structure সম্পর্কে দ্রুত ধারণা দেয়।

আপনি দেখতে পাবেন —

*   Columns–এর নাম
*   Values–এর pattern
*   Data type সম্পর্কে প্রাথমিক ধারণা (যেমন numeric, text, date ইত্যাদি)
*   Missing value আছে কি না তার চোখে পড়ার মতো এক ঝলক

একটি সাধারণ table–এর মতো output দেখতে এমন হবে:

```c
| col1 | col2 | col3 | ... |
|------|------|------|-----|
| 12 | Male | 5.6 | ... |
| 15 | Female | 4.2 | ... |
| ... | ... | ... | ... |
```

এখান থেকেই আপনি EDA–র journey শুরু করেন। Dataset ছোট হোক বা বড়, প্রথম visuals বা first look dataset–এর পরবর্তী analysis direction নির্ধারণ করতে সাহায্য করে।

## Summary

এই introductory lesson–এ আপনি শিখলেন —

*   EDA হলো Machine Learning–এর একটি critical step, যা data–কে বুঝে modeling–এর জন্য প্রস্তুত করে।
*   Raw data–তে outlier, missing values, imbalance ইত্যাদি সমস্যা থাকতে পারে এবং এগুলোর সমাধান EDA–র মাধ্যমে বের করা হয়।
*   Notebook–এর basic setup কীভাবে শুরু করতে হয় এবং কোন কোন Python libraries সাধারণত ব্যবহৃত হয়।
*   Data visualization এবং data inspection–এর মাধ্যমে dataset–এ কী আছে, কী নেই এবং কোথায় সমস্যা তা preliminaryভাবে বোঝা যায়।

এখন আপনি প্রস্তুত **6.2: How to Find and Select a Dataset for ML Project** lesson–এ যাওয়ার জন্য, যেখানে আপনি শিখবেন কোন ধরনের dataset কোন ধরনের ML problem–এর জন্য উপযুক্ত এবং dataset selection–এর সঠিক কৌশল।

===========================================================

6-1 Introduction

## Learning Objectives

এই ভিডিওটির শেষে আপনি —

*   Exploratory Data Analysis (EDA) কী এবং কেন গুরুত্বপূর্ণ তা গভীরভাবে বুঝতে পারবেন।
*   Machine Learning workflow–এর কোন ধাপে EDA আসে এবং কেন model-building এর আগে এটি অপরিহার্য, তা ব্যাখ্যা করতে পারবেন।
*   Dataset বুঝে নেওয়ার জন্য কোন কোন ধরনের প্রশ্ন, পরীক্ষা এবং observation প্রয়োজন হয় তা বিস্তারিতভাবে শিখবেন।
*   Module 06–এ ঠিক কী কী skills অর্জন করবেন এবং প্রতিটি section কী নিয়ে কাজ করবে তার একটি স্পষ্ট roadmap পাবেন।
*   Data-এর quality, structure এবং problem–specific characteristics যাচাই করার প্রাথমিক mindset তৈরি হবে।

## Step-by-Step Explanation

Exploratory Data Analysis (EDA) হলো Machine Learning project–এর foundational step। এই ধাপ ছাড়া modeling শুরু করাটা অনেকটা অন্ধভাবে ভবিষ্যদ্বাণী করার মতো। কারণ EDA–র মূল লক্ষ্য হলো — data–কে বোঝা, এর pattern–গুলো খুঁজে বের করা, সমস্যা শনাক্ত করা এবং model–এর আগে dataset–কে এমনভাবে প্রস্তুত করা যাতে model কার্যকরভাবে শিখতে পারে।

EDA করার সময় একজন Data Scientist সাধারণত dataset–এর নিচের বিষয়গুলো পর্যবেক্ষণ করেন —

*   Dataset–এ total কতটি row ও column আছে (shape)।
*   প্রতিটি feature কী ধরনের data ধারণ করে (data types)।
*   Missing values কোন কোন column–এ আছে এবং সেগুলোর পরিমাণ কত।
*   Numerical feature–গুলোর distribution symmetric নাকি skewed।
*   Categorical feature–গুলোতে imbalance আছে কি না।
*   Target variable-এর সাথে কোন feature-এর logical বা statistical সম্পর্ক থাকতে পারে।

EDA–র মূল উদ্দেশ্য হলো data–কে readable, understandable এবং usable করে তোলা। এর মাধ্যমে আপনি data–র hidden structure, pattern এবং সমস্যাগুলোর একটি clear ছবি পান。

### ⭐ কেন EDA এত গুরুত্বপূর্ণ?

Machine Learning model তখনই ভালো perform করে যখন dataset সঠিকভাবে clean, formatted এবং balanced থাকে। Raw data–তে সাধারণত কিছু সমস্যা থাকেই —

*   Outliers model-এর prediction–এ noise তৈরি করে।
*   Missing values model–কে ভুল pattern শিখায়।
*   Skewed distribution model–কে biased করে তোলে।
*   Unnecessary বা irrelevant feature model–এর complexity বাড়ায় কিন্তু performance কমায়।

EDA–র মাধ্যমে আপনি —

*   Outlier detection করতে পারেন।
*   Missing value–এর ধরন অনুযায়ী Imputation technique পরিকল্পনা করতে পারেন।
*   Distribution দেখে transformation (log/sqrt) দরকার কি না ঠিক করতে পারেন।
*   Feature selection এবং engineering–এর শুরুটা শক্তভাবে করতে পারেন।

EDA data understanding–এর সেতুবন্ধ তৈরি করে, যা ছাড়া model-building পুরোপুরি incomplete থেকে যায়।

## Code Blocks (Notebook থেকে নেওয়া)

> **Note:** এই introductory ভিডিওতে সাধারণত heavy কোড থাকে না, কিন্তু notebook–এ EDA শুরু করার জন্য একটি basic setup দেওয়া থাকে। সেই সেটআপ অপরিবর্তিত অবস্থায় নিচে যুক্ত হলো।

```c
# Basic Imports for EDA Module
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

# Settings
df = pd.read_csv('your_dataset.csv')
df.head()
```

এখানে ব্যবহৃত libraries—

*   **pandas**: data manipulation
*   **numpy**: numerical operations
*   **matplotlib** এবং **seaborn**: visualization

Notebook–এ যদি অতিরিক্ত initialization, style settings বা dataset loading থাকে, পরবর্তী lesson–এ সেগুলো বিশদভাবে ব্যাখ্যা করা হবে।

## Outputs / Charts Explanation

উপরের কোড রান করলে সাধারণত প্রথম যে আউটপুটটি পাওয়া যায় তা হলো — `df.head()`। এটি dataset–এর প্রথম পাঁচটি row দেখায়, যা আপনাকে data–এর structure সম্পর্কে দ্রুত ধারণা দেয়।

আপনি দেখতে পাবেন —

*   Columns–এর নাম
*   Values–এর pattern
*   Data type সম্পর্কে প্রাথমিক ধারণা (যেমন numeric, text, date ইত্যাদি)
*   Missing value আছে কি না তার চোখে পড়ার মতো এক ঝলক

একটি সাধারণ table–এর মতো output দেখতে এমন হবে:

```c
| col1 | col2 | col3 | ... |
|------|------|------|-----|
| 12 | Male | 5.6 | ... |
| 15 | Female | 4.2 | ... |
| ... | ... | ... | ... |
```

এখান থেকেই আপনি EDA–র journey শুরু করেন। Dataset ছোট হোক বা বড়, প্রথম visuals বা first look dataset–এর পরবর্তী analysis direction নির্ধারণ করতে সাহায্য করে।

## Summary

এই introductory lesson–এ আপনি শিখলেন —

*   EDA হলো Machine Learning–এর একটি critical step, যা data–কে বুঝে modeling–এর জন্য প্রস্তুত করে।
*   Raw data–তে outlier, missing values, imbalance ইত্যাদি সমস্যা থাকতে পারে এবং এগুলোর সমাধান EDA–র মাধ্যমে বের করা হয়।
*   Notebook–এর basic setup কীভাবে শুরু করতে হয় এবং কোন কোন Python libraries সাধারণত ব্যবহৃত হয়।
*   Data visualization এবং data inspection–এর মাধ্যমে dataset–এ কী আছে, কী নেই

======================================================


## 6.3 Setup and Data Loading

## Learning Objectives

এই লেসন শেষে আপনি —

*   EDA শুরু করার জন্য প্রয়োজনীয় Python environment এবং libraries প্রস্তুত করতে পারবেন।
*   Notebook–এ ব্যবহৃত imports, file upload process এবং তাদের উদ্দেশ্য বুঝতে পারবেন।
*   heart.csv dataset সঠিকভাবে load করার Colab ও Local উভয় পদ্ধতি শিখবেন।
*   DataFrame–এর প্রাথমিক structure যাচাই করার পদ্ধতি (head, info, shape, describe) ব্যাখ্যা করতে পারবেন।
*   Target, numeric এবং categorical columns define করার গুরুত্ব বুঝতে পারবেন।

## Step-by-Step Explanation

এই মডিউলে আমরা EDA শুরু করার আগে notebook–এ ব্যবহৃত ঠিক একই ধাপগুলো অনুসরণ করব। Colab notebook–এ dataset upload, load এবং preview যেভাবে করা হয়েছে সেটিই GitBook–এ প্রতিফলিত করা হলো।

### Step 1: Import Necessary Libraries

Notebook–এ ব্যবহৃত libraries:

*   **pandas** — data manipulation
*   **numpy** — numerical computation
*   **matplotlib** — plotting
*   **seaborn** — visualization

## Code (Notebook থেকে নেওয়া)

### 1. Import Libraries

```python
import numpy as np
import pandas as pd

import matplotlib.pyplot as plt
import seaborn as sns

plt.style.use("default")
sns.set(font_scale=1.1)
```

**Explanation:**

*   `numpy` → numerical computation
*   `pandas` → tabular data analysis
*   `matplotlib` → plotting
*   `seaborn` → statistical visualization
*   `plt.style.use("default")` → default plotting style
*   `sns.set(font_scale=1.1)` → plots–এ font size একটু বড় ও readable করা

### 2. Upload & Load Dataset (Colab Method)

```python
from google.colab import files

# Upload heart.csv from your computer
uploaded = files.upload()

# The Kaggle file is named "heart.csv"
df = pd.read_csv("heart.csv")

df.head(10)
```

**Explanation:**

*   `files.upload()` → Colab–এ local file upload করার জন্য
*   ফাইল upload হলে সেটি session–এ save হয়
*   `pd.read_csv("heart.csv")` → uploaded CSV ফাইল read করে DataFrame তৈরি করে
*   `df.head(10)` → প্রথম ১০টি row দেখায়, dataset structure যাচাই করতে সাহায্য করে

**Output:**

[Image]

### Alternative (Local Machine)

```python
df = pd.read_csv("heart.csv")
df.head(10)
```

**Explanation:**

Colab নয়, local environment–এ কাজ করলে upload দরকার নেই — ফাইলের path দিলেই read হবে।

### 3. Initial Dataset Check

```python
print("Shape: ", df.shape)

print("Info: ")
df.info()

df.describe().T
```

**Explanation:**

*   `df.shape` → total rows ও columns
*   `df.info()` → data types, non-null count, memory usage
*   `df.describe().T` → numerical summary statistics (mean, std, min, max)

**Output:**
![[Pasted image 20251121214511.png]]
[]![[Pasted image 20251121214527.png]]
![[Pasted image 20251121214532.png]]

![[Pasted image 20251121214539.png]]


### 4. Define Target, Numeric & Categorical Columns

```python
target_col = "HeartDisease"

numeric_cols = ["Age", "RestingBP", "Cholesterol", "FastingBS",
                "MaxHR", "Oldpeak"]

categorical_cols = ["Sex", "ChestPainType", "RestingECG",
                    "ExerciseAngina", "ST_Slope"]

print("Target:", target_col)
print("Numeric columns:", numeric_cols)
print("Categorical columns:", categorical_cols)
```

**Explanation:**

*   `target_col` → prediction target column
*   `numeric_cols` → numerical analysis (histogram, boxplot, correlation)–এ ব্যবহৃত columns
*   `categorical_cols` → bar chart, countplot, crosstab–এ ব্যবহৃত columns
*   এই separation EDA–কে structured করে

**Output:**


![[Pasted image 20251121214548.png]]
## Summary

এই লেসনে আপনি শিখলেন —

*   Notebook–এ ব্যবহৃত exact imports এবং visualization settings।
*   Colab–এ heart.csv upload করে load করার পদ্ধতি।
*   Dataset preview ও structure যাচাই (head, info, describe)।
*   Target, numeric এবং categorical columns define করার প্রয়োজনীয়তা।
==========================================================

6.4 Missing Values and Basic Quality Checks

## Learning Objectives

এই লেসনের শেষে আপনি —

*   heart.csv dataset–এ missing values আছে কি না তা যাচাই করতে পারবেন।
*   Numeric features–এর minimum, maximum, mean এবং median values পরীক্ষা করতে পারবেন।
*   Categorical features–এর unique values দেখে data consistency যাচাই করতে পারবেন।
*   Notebook–এ ব্যবহৃত exact কোড অনুসারে data quality assessment করতে পারবেন।

## Step-by-Step Explanation

Notebook–এর এই অংশে আমরা দুটি প্রধান কাজ করি:

1.  **Missing value detection** — dataset–এ কোনো ফাঁকা/অনুপস্থিত মান আছে কি না তা দেখা।
2.  **Basic quality checks** — numeric value ranges পরীক্ষা করা এবং categorical features–এ কী কী মান আছে তা দেখা।

এই ধাপগুলো model–এর আগে data cleaning–এর প্রয়োজন আছে কি না তা স্পষ্ট করে।

## Code (Notebook থেকে নেওয়া)

### 1. Missing Values Check

```python
df.isna().sum()
```

**Explanation:**

*   প্রতিটি column–এ কতটি missing value আছে তা গণনা করে।
*   heart.csv dataset–এ সাধারণত missing value থাকে না, কিন্তু যাচাই করা সর্বদা গুরুত্বপূর্ণ।

**Output:**

[Image]![[Pasted image 20251121214917.png]]

### 2. Numeric Value Range Check

```python
df[numeric_cols].agg(["min", "max", "mean", "median"]).T
```

**Explanation:**

*   Numerical features–এর min, max, mean এবং median দেখায়।
*   এর মাধ্যমে extreme values বা suspicious entries (যেমন 0 cholesterol) শনাক্ত করা যায়।

**Output:**

[Image]
![[Pasted image 20251121214924.png]]
### 3. Unique Values of Categorical Columns

```python
for c in categorical_cols:
  print(c, df[c].unique())
```

**Explanation:**

*   প্রতিটি categorical feature–এর unique labels দেখায়।
*   Inconsistent labels (যেমন ‘M’ এবং ‘Male’ মিশে যাওয়া) সহজে ধরা যায়।

**Output:**

[Image]
![[Pasted image 20251121214930.png]]
## Summary

এই লেসনে আপনি শিখলেন —

*   Missing values detect করার পদ্ধতি।
*   Numeric features–এর range দেখে suspicious values শনাক্ত করা।
*   Categorical columns–এর unique values দেখে data consistency যাচাই করা।
====================================================

## 6.5 Understanding Distributions with Histograms and Boxplots (Numerical Features)

## Learning Objectives

এই লেসনের শেষে আপনি —

*   Numerical features–এর distribution বুঝতে পারবেন।
*   Histogram ব্যবহার করে value–গুলোর frequency, shape এবং spread পর্যবেক্ষণ করতে পারবেন।
*   Boxplot ব্যবহার করে median, quartile এবং potential outliers শনাক্ত করতে পারবেন।
*   Notebook–এ ব্যবহৃত exact কোড ব্যবহার করে heart.csv dataset–এর numerical behavior বিশ্লেষণ করতে পারবেন।

## Step-by-Step Explanation

এই অংশে আমরা numerical feature–গুলোর distribution visualভাবে বুঝব।

### Histogram

Histogram কোনো numerical feature–এর মান কতবার পাওয়া যাচ্ছে তা দেখায় —

*   distribution symmetric নাকি skewed
*   কোন মানগুলো বেশি common
*   extreme values আছে কি না

### Boxplot

Boxplot দেখায় —

*   median
*   data spread
*   quartiles
*   outliers

Notebook–এ numerical columns নির্ধারণ করা আছে:

```python
["Age", "RestingBP", "Cholesterol", "FastingBS", "MaxHR", "Oldpeak"]
```

## Code (Notebook থেকে নেওয়া)

### 1. Histogram for Age

```python
df["Age"].hist(bins=20, figsize=(8,4))
plt.suptitle("Histrograms of Numerical Feature Age", fontsize = 14)
plt.tight_layout()
plt.show()
```

**Explanation:**

Age distribution কেমন এবং কোন বয়সের গ্রুপ বেশি — তা দেখা যায়।

**Output:**

[Image]
![[Pasted image 20251121215047.png]]
### 2. Histogram for RestingBP

```python
df["RestingBP"].hist(bins=20, figsize=(8,4))
plt.suptitle("Histrograms of Numerical Feature RestingBP", fontsize = 14)
plt.tight_layout()
plt.show()
```

**Explanation:**

Resting blood pressure কোন range–এ বেশি পাওয়া যায় তা বোঝা যায়।

**Output:**

[Image]
![[Pasted image 20251121215052.png]]
### 3. Histogram for Cholesterol

```python
df["Cholesterol"].hist(bins=20, figsize=(8,4))
plt.suptitle("Histrograms of Numerical Feature Cholesterol", fontsize = 14)
plt.tight_layout()
plt.show()
```

**Explanation:**

Cholesterol মানগুলোর distribution দেখা যায় — বিশেষ করে zero value detect করা যায়।

**Output:**

[Image]
![[Pasted image 20251121215056.png]]
### 4. Combined Histogram for All Numerical Features

```python
df[numeric_cols].hist(bins=20, figsize=(12,8))
plt.suptitle("Histrograms of Numerical Features", fontsize = 14)
plt.tight_layout()
plt.show()
```

**Explanation:**

একই grid–এ সব numerical features–এর distribution compare করা যায়।

**Output:**

[Image]
![[Pasted image 20251121215101.png]]
### 5. Boxplot for All Numerical Features

```python
plt.figure(figsize=(12,8))
df[numeric_cols].boxplot()
plt.title("Boxplot of Numerical Features")
plt.xticks(rotation=45)
plt.show()
```

**Explanation:**

Numerical features–এর spread ও potential outliers একসাথে দেখা যায়।

**Output:**

[Image]
![[Pasted image 20251121215107.png]]
### 6. Boxplot for a Single Feature (MaxHR)

```python
plt.figure(figsize=(12,8))
df.boxplot(column=numeric_cols[4])
plt.title("Boxplot of Numerical Features")
plt.xticks(rotation=45)
plt.show()
```

**Explanation:**

MaxHR feature–এর আলাদা distribution ও outliers highlight করা হয়েছে।

**Output:**

[Image]
![[Pasted image 20251121215112.png]]
## Summary

এই লেসনে আপনি শিখলেন —

*   Histogram ব্যবহার করে numerical distribution visualization।
*   Boxplot দিয়ে spread ও outlier শনাক্ত করার পদ্ধতি।
*   heart.csv dataset–এর numerical features–এর আচরণ কেমন তা plot–এর মাধ্যমে বোঝা।

============================================================


6.6 Categorical Feature Exploration

## Learning Objectives

এই লেসনের শেষে আপনি —

*   heart.csv dataset–এর categorical features সনাক্ত করতে পারবেন।
*   প্রতিটি categorical column–এর value distribution bar chart–এর মাধ্যমে বুঝতে পারবেন।
*   Rare categories, dominant categories এবং inconsistent values চিহ্নিত করতে পারবেন।
*   Notebook–এ ব্যবহৃত exact visualization code ব্যবহার করে categorical EDA করতে পারবেন।

## Step-by-Step Explanation

Categorical EDA–র মূল উদ্দেশ্য হলো প্রতিটি category কতবার দেখা যাচ্ছে তা বোঝা। এটি পরে encoding, grouping এবং feature engineering–এ সাহায্য করে।

Notebook–এ categorical columns নির্ধারণ করা হয়েছে:

```python
["Sex", "ChestPainType", "RestingECG", "ExerciseAngina", "ST_Slope"]
```

Categorical EDA–তে আমরা —

*   Category frequency
*   Category imbalance
*   Category meaning
*   Rare categories

এসব বিশ্লেষণ করি।

## Code (Notebook থেকে নেওয়া)

### 1. Value Counts Bar Chart for Each Categorical Feature

```python
for c in categorical_cols:
  plt.figure(figsize=(5,4))
  df[c].value_counts().plot(kind="bar")
  plt.title(f"Value counts for {c}")
  plt.ylabel("Count")
  plt.tight_layout()
  plt.show()
```

**Explanation:**

*   প্রতিটি categorical column–এর জন্য value_counts() ব্যবহার করে কতবার কোন category এসেছে তা দেখা হয়।
*   Bar chart category imbalance দ্রুত বোঝায়।
*   Small categories (rare categories) ভবিষ্যতে grouping বা encoding–এ বিশেষভাবে বিবেচনা করা হতে পারে।

**Output:**

![[Pasted image 20251121215219.png]]
## Summary

এই লেসনে আপনি শিখলেন —

*   Categorical features–এর value distribution bar chart–এর মাধ্যমে বিশ্লেষণ করা।
*   Rare categories এবং dominant categories detect করা।
*   Notebook–এর exact categorical visualization code ব্যবহার করা।

=========================================================

6.7 Relationships Between Features and Target (Part 01)

## Learning Objectives

এই লেসনের শেষে আপনি —

*   Numerical features এবং target variable (HeartDisease)–এর মধ্যে সম্পর্ক visualভাবে বুঝতে পারবেন।
*   Boxplot ব্যবহার করে class-wise distribution differences বিশ্লেষণ করতে পারবেন।
*   কোন numerical feature target–এর সাথে strong relationship দেখাচ্ছে তা শনাক্ত করতে পারবেন।
*   Notebook–এ ব্যবহৃত exact boxplot loop–এর ব্যাখ্যা বুঝতে পারবেন।

## Step-by-Step Explanation

Notebook–এর এই অংশে numerical features–এর প্রতিটির সাথে target (HeartDisease) কীভাবে সম্পর্কিত তা visualization–এর মাধ্যমে পরীক্ষা করা হয়।

এটি binary classification problem হওয়ায় boxplot খুবই কার্যকর:

*   HeartDisease = 0 (No)
*   HeartDisease = 1 (Yes)

যদি কোনো feature–এর distribution এই দুই class–এ অনেক different হয় → এটি শক্তিশালী predictive feature।

Notebook–এ numerical features নির্ধারিত:

```c
["Age", "RestingBP", "Cholesterol", "FastingBS", "MaxHR", "Oldpeak"]
```

## Code (Notebook থেকে নেওয়া)

### 1. Boxplots of Numerical Features Split by HeartDisease

```python
plt.figure(figsize=(12,8))
for i, col in enumerate(numeric_cols, 1):
  plt.subplot(2,3,i)
  sns.boxplot(x=df[target_col], y = df[col])
  plt.title(col)

plt.tight_layout()
plt.show()
```

**Explanation:**

*   Loop ব্যবহার করে প্রতিটি numerical feature–এর জন্য boxplot তৈরি করা হচ্ছে।
*   `x=df[target_col]` → HeartDisease = 0 এবং 1 অনুযায়ী separation।
*   `y=df[col]` → feature value distribution।
*   Subplot grid → (2 rows × 3 columns) সব feature–কে এক layout–এ দেখানোর জন্য।

**Output:**

![[Pasted image 20251121215327.png]]
## Insight

Notebook–এ উল্লেখ করা হয়েছে:

> Large differences in distributions between target classes often signal strong predictive potential.

অর্থাৎ —

*   যদি HeartDisease = 1 হলে MaxHR এবং Oldpeak–এর distribution significantly different হয় → এই দুটি feature ML model–এ শক্তিশালী সংকেত দেয়।

## Summary

এই লেসনে আপনি শিখলেন —

*   Numerical features এবং HeartDisease target–এর সম্পর্ক boxplot দিয়ে দেখা।
*   Subplot grid ব্যবহার করে multiple feature comparison করা।
*   কোন feature target–এর সাথে strong predictive behavior দেখায় তা visualভাবে শনাক্ত করা।

==============================================================


6.8 Relationships Between Features and Target (Part 02 – Correlation Matrix)

## Learning Objectives

এই লেসনের শেষে আপনি —

*   Numerical features–এর correlation বুঝতে পারবেন।
*   Correlation matrix তৈরি করতে পারবেন।
*   Heatmap ব্যবহার করে feature–গুলোর মধ্যে relationship visualভাবে ধরতে পারবেন।
*   কোন features target (HeartDisease)–এর সাথে stronger association দেখাচ্ছে তা সনাক্ত করতে পারবেন।
*   Notebook–এ ব্যবহৃত exact code block ও তার অর্থ বুঝতে পারবেন।

## Step-by-Step Explanation

Correlation বোঝায়:

*   দুইটি numerical variable কতটা একে অপরের সাথে সম্পর্কযুক্ত।
*   Positive correlation → একটার মান বাড়লে আরেকটাও বাড়ে।
*   Negative correlation → একটার মান বাড়লে আরেকটা কমে।

Notebook–এ numerical features + target মিলিয়ে একটি correlation matrix তৈরি করা হয়েছে:

```c
[Age, RestingBP, Cholesterol, FastingBS, MaxHR, Oldpeak, HeartDisease]
```

Heatmap ব্যবহার করে visually দেখা যায় — কোন feature target–এর সাথে বেশি যুক্ত।

## Code (Notebook থেকে নেওয়া)

### 1. Correlation Matrix তৈরি

```python
corr_matrix = df[numeric_cols + [target_col]].corr()
```

**Explanation:**

*   Numerical features + target একসাথে correlational analysis করা হয়েছে।
*   `.corr()` → Pearson correlation coefficient গণনা করে।

### 2. Correlation Heatmap

```python
plt.figure(figsize=(8,6))
sns.heatmap(corr_matrix, annot=True, cmap="coolwarm", fmt=".2f", linewidths=0.5)
plt.title("Correlation Heatmap for Numerical Features")
plt.tight_layout()
plt.show()
```

**Explanation:**

*   `annot=True` → প্রতিটি cell–এ numeric correlation মান দেখায়।
*   `coolwarm` colormap → high vs low correlation সহজে interpret করা যায়।
*   `fmt=".2f"` → দুই decimal place–এ values প্রদর্শন।

**Output:**
![[Pasted image 20251121215450.png]]


### 3. Correlation Values Sorted by Target

```python
corr_matrix[target_col].sort_values(ascending=False)
```

**Explanation:**

*   Target variable-এর সাথে কোন features সবচেয়ে বেশি correlated → তা descending order–এ দেখায়।
*   Modeling–এ feature selection বা feature importance intuition পেতে সাহায্য করে।

**Output:**


![[Pasted image 20251121215458.png]]
## Insight

Notebook–এ উল্লেখ করা হয়েছে:

> High absolute correlation with the target is interesting, but do not ignore low correlation features.

এর মানে —

*   শুধু high correlation মানেই important নয়।
*   Low correlation থাকা features অন্য features–এর সাথে মিলিয়ে গুরুত্বপূর্ণ হতে পারে।

## Summary

এই লেসনে আপনি —

*   Correlation matrix তৈরি করা
*   Heatmap ব্যবহার করে relationship visualization
*   Target–এর সাথে কোন features strongest association দেখায় তা দেখা
*   Correlation interpretation–এর practical guideline বুঝেছেন

===============================================================

6.9 Categorical Features vs Target

## Learning Objectives

এই লেসনের শেষে আপনি —

*   Categorical feature–গুলোর সাথে target (HeartDisease)–এর সম্পর্ক বুঝতে পারবেন।
*   Crosstab ও bar chart ব্যবহার করে class proportion visual করতে শিখবেন।
*   কোন categorical feature target class–এ strong separation দেখায় তা সনাক্ত করতে পারবেন।
*   Notebook–এ ব্যবহৃত exact কোডগুলোর উদ্দেশ্য ও আউটপুট ব্যাখ্যা করতে পারবেন।

## Step-by-Step Explanation

Categorical feature–গুলোর target class–এ কীভাবে distribution পরিবর্তিত হচ্ছে তা বোঝা অত্যন্ত গুরুত্বপূর্ণ।

Notebook–এ ব্যবহৃত পদ্ধতি:

1.  প্রতিটি categorical feature-এর জন্য **crosstab** তৈরি করা
2.  Crosstab কে **row-normalized proportion** হিসেবে দেখা
3.  Stacked bar chart–এ visualization

এতে বোঝা যায় —

*   কোন category–তে HeartDisease rate বেশি
*   কোনগুলোতে কম
*   কোন categorical features decision boundary তৈরিতে সহায়তা করতে পারে

Notebook–এ categorical features:

```python
["Sex", "ChestPainType", "RestingECG", "ExerciseAngina", "ST_Slope"]
```

## Code (Notebook থেকে নেওয়া)

### 1. Crosstab + Visualization for Each Categorical Feature

```python
for c in categorical_cols:
  ct = pd.crosstab(df[c], df[target_col], normalize="index")
  print(f"\nProportion of HeartDisease within {c}")
  display(ct)

  ct.plot(kind="bar", stacked=True, figsize=(6,4))
  plt.title(f"HeartDisease proportion by {c}")
  plt.ylabel("proportion")
  plt.xticks(rotation=0)
  plt.tight_layout()
  plt.show()
```

**Explanation:**

*   `pd.crosstab(..., normalize="index")` → প্রতিটি category–এর মধ্যে HeartDisease=0 এবং 1 কী proportion তা দেখায়।
*   `display(ct)` → proportion table print করে।
*   `stacked=True` → stacked bar chart proportion comparison সহজ করে।
*   Each categorical column–এর জন্য loop → সব features sequentially analyze করা হয়।

**Output:**

![[Pasted image 20251121215612.png]]
![[Pasted image 20251121215619.png]]
![[Pasted image 20251121215624.png]]
![[Pasted image 20251121215629.png]]
[Image]
![[Pasted image 20251121215710.png]]
[Image]

[Image]

## Insight

Notebook–এ উল্লেখ করা হয়েছে:

> Crosstabs normalized by row are very helpful.

অর্থাৎ —

*   Category–wise HeartDisease rate visualভাবে বুঝতে হলে row-based proportion সবচেয়ে তথ্যবহ।
*   Example: ASY chest pain–এ HeartDisease rate অত্যন্ত বেশি → এটি একটি strong predictive feature।

## Summary

এই লেসনে আপনি শিখলেন —

*   Crosstab ব্যবহার করে categorical feature vs target proportion দেখা।
*   Stacked bar chart দিয়ে category–wise HeartDisease rate visualize করা।
*   কোন categorical features target separation–এ গুরুত্বপূর্ণ ভূমিকা রাখে তা চিহ্নিত করা।

Previous
6.8 Relationships Between Features and Target (Part 02 – Correlation Matrix)

Next
Module 07: Data Preprocessing and Feature Engineering

Last updated a day ago


