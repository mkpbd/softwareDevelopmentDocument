
```txt
ACT AS a Principal Database Engineer with 35+ years of experience in multi-tenant healthcare ERP systems. You have built platforms for 100+ hospital groups (each with 5–50 branches) across 6 countries, handling HIPAA, GDPR, NABH, JCI, and local data residency laws.

TASK: Design a production-ready, multi-tenant, multi-branch relational database schema for a Hospital ERP system. The system must support 63 domains (original 48 + 15 added). The core architectural requirement:

- A TENANT represents an independent hospital group (e.g., "Apollo Hospitals Group").
- A FACILITY (branch) represents a physical hospital or clinic location belonging to a tenant (e.g., "Apollo Chennai", "Apollo Hyderabad").
- All data (patients, encounters, billing, inventory, HR) must be isolated at least at the tenant level, with optional isolation or sharing at the facility level based on business rules.

## ADDITIONAL MULTI-TENANT REQUIREMENTS

### 1. Tenant & Facility Tables
- `tenant` (id, name, subscription_tier, timezone, data_retention_days, is_active)
- `facility` (id, tenant_id, facility_code, name, address_json, license_number, bed_capacity, is_active)
- Every major business table must include `tenant_id` and `facility_id` as foreign keys or as part of the primary key (for distributed sharding).
- For tables that logically span facilities (e.g., patient master, insurance contracts), include `tenant_id` only – `facility_id` is nullable or moved to a bridge table.

### 2. Data Isolation & Row-Level Security
- Row-Level Security (RLS) must enforce:
  - Tenant A users cannot see Tenant B data.
  - Within a tenant, users can be restricted to specific facilities (e.g., a nurse at Chennai sees only Chennai patients).
  - Corporate users (e.g., group CFO) can see aggregated data across all facilities of the tenant.
- Provide RLS policies using `tenant_id` and `facility_id` via session variables (`current_setting('app.tenant_id')`, `current_setting('app.facility_ids')`).

### 3. Sharding & Partitioning Strategy
- Use `tenant_id` as the **first sharding key** for Citus distributed tables.
- Use `facility_id` as the **second distribution column** or as a partition key for range/list partitioning.
- For single-server PostgreSQL, partition large tables by `(tenant_id, facility_id, date_range)` using list+range composite partitioning.

### 4. Cross-Facility Queries & Reporting
- Provide indexed views or materialized views for:
  - Group-level daily revenue (`tenant_id`, `facility_id`, `date`)
  - Facility occupancy ratios (`tenant_id`, `facility_id`, `ward`, `timestamp`)
  - Inter-facility patient referrals (patient transferred from Facility A to Facility B within same tenant)
- Include a `patient_consolidated` table that links patient records across facilities using a global `tenant_patient_id` (matching via national ID, phone, or biometric).

### 5. Master Data Management (MDM)
- Global master data (e.g., ICD-11 codes, LOINC, NDC drug database) is tenant-agnostic – stored in `global_mdm` schema.
- Tenant-specific master data (e.g., price lists, insurance panels, vendor contracts) must include `tenant_id` and optionally `facility_id` for location-specific overrides.
- Provide a `facility_item_price` table that overrides the tenant-wide price list.

### 6. Audit & Compliance for Multi-Tenant
- Audit logs must include `tenant_id` and `facility_id` to enable forensic analysis per tenant.
- Data retention policies must be configurable per tenant (e.g., Tenant A retains clinical data 20 years, Tenant B retains 10 years). Implement a scheduled partition drop job that reads `tenant.data_retention_days`.

## FULL REQUIREMENTS (inherited from previous prompt, adjusted for multi-tenant)

1. **ENTITIES & RELATIONSHIPS** – Generate ER diagram in Mermaid showing `tenant`, `facility`, and how they connect to `patient_master`, `encounter`, `billing_item_line`, etc.

2. **CRITICAL TABLES (DDL)** – Include `tenant` and `facility` tables plus:
   - `patient_master` (with `tenant_id`, optional `facility_id` for home facility, soft delete, MRN merging across facilities)
   - `encounter` (must have `tenant_id`, `facility_id`, `admission_date` – partitioned by tenant+facility+date)
   - `clinical_notes` (encrypted, with tenant isolation)
   - `order_entry` (with lifecycle status)
   - `billing_item_line` (splits for insurance/corporate/patient, tenant-specific pricing)
   - `audit_log` (immutable, includes `tenant_id`, `facility_id`)

3. **DATA INTEGRITY** – CHECK constraints, foreign keys with `ON DELETE RESTRICT`, composite natural keys scoped to `(tenant_id, facility_id, ...)`.

4. **PERFORMANCE & SCALABILITY** – Partitioning strategy by `(tenant_id, facility_id, date)`. Index recommendations for multi-tenant search (e.g., `(tenant_id, facility_id, status)`). Sharding key = `tenant_id`.

5. **SECURITY & COMPLIANCE** – Column-level encryption per tenant (different encryption keys per tenant). RLS policies that check `tenant_id` from session. Separate audit schema with triggers.

6. **INTEGRATION READY** – HL7/FHIR message tracking includes `tenant_id` and `facility_id`. JSONB columns for extensions.

7. **TEMPORAL FEATURES** – System-versioned tables with `valid_time` and `transaction_time`, scoped by tenant.

8. **OUTPUT FORMAT**:
   - Section 1: ER diagram (Mermaid) showing multi-tenant hierarchy.
   - Section 2: DDL for top 25 tables (add `tenant`, `facility`, `patient_consolidated`).
   - Section 3: Index scripts for cross-facility reporting and tenant isolation.
   - Section 4: Partitioning strategy example (list of tenants + range by date).
   - Section 5: RLS policies and encryption approach (per-tenant keys).
   - Section 6: Sample queries with EXPLAIN ANALYZE for:
        a) Active inpatient census for a specific facility.
        b) Daily revenue for all facilities of a tenant (grouped by facility).
        c) Patient transfer between two facilities (referral tracking).

**ASSUMPTIONS**:
- Number of tenants: 10–500 (each with 1–50 facilities).
- Workload: 70% read / 30% write, but cross-tenant reads are never allowed (only cross-facility within tenant).
- Peak concurrent users: 5000 across all tenants, max 2000 per tenant.
- Data retention: Configurable per tenant (clinical forever, financial 10 years, logs 7 years min).
- High availability per tenant: RPO < 5 min, RTO < 30 min (shared infrastructure or separate clusters based on tenant tier).

DELIVER production-ready, documented, and tested schema with multi-tenant isolation as the highest priority.
```