#### **Module 7: Security: Identity & Auth ( The Bouncers)**
1.  Authentication vs Authorization (Who vs What)
2.  JWT Basics (The VIP Pass)
3.  Setting up ASP.NET Core Identity
4.  Registration & Login Logic
5.  Generating JWT Tokens
6.  Authorize Attribute & Policies
7.  Role-Based Access Control (RBAC)
8.  Claims-Based Authorization
9.  Refresh Tokens (Keeping session alive)
10. **Coding Exercise:** Login System with JWT.

স্বাগতম **Module 7: Security: Identity & Auth (The Bouncers)**-এ! 🕶️🔒

বস, এতক্ষণ আমরা রেস্টুরেন্ট সাজিয়েছি, ওয়েটার রেখেছি, কিচেন সেট করেছি। এখন কথা হলো, "বিল না দিয়ে বা বিরিয়ানি খেয়ে পালালে ধরবে কে?" অথবা "কিচেনে কাস্টমার ঢুকে পড়লে আটকাবে কে?"

আমাদের দরকার **Bouncers** (সিকিউরিটি গার্ড)। ওয়েব দুনিয়ায় এই বাউন্সার হলো **Authentication** (চিনবে কে তুমি) এবং **Authorization** (তোমার ক্ষমতা কতটুকু)।

.NET-এ এটা করা হয় **ASP.NET Core Identity** এবং **JWT (JSON Web Token)** দিয়ে। এটা এই কোর্সের **সবচেয়ে ক্রিটিকাল** মডিউল। মনোযোগ দিয়ে দেখো, নাহলে হ্যাকাররা তোমার সাইট হ্যাক করে "I Love You" লিখে রেখে যাবে! 💀

---

#### **Lesson 7.1: Authentication vs Authorization (Who vs What)**

এই দুইটা শব্দে সবাই প্যাঁচ লাগায়। সহজ উদাহরণ দেখো:

১. **Authentication (AuthN):** তুমি যখন এয়ারপোর্টে যাও, পাসপোর্ট চেক করে কনফার্ম করে "তুমিই রহিম"।
   *   *প্রশ্ন:* কে তুমি? (Who are you?)
   *   *পদ্ধতি:* Login (Username + Password).

২. **Authorization (AuthZ):** তুমি প্লেনে উঠেছো, কিন্তু তুমি কি ককপিটে ঢুকে প্লেন চালাতে পারবে? না! তুমি শুধু ইকোনমি সিটে বসতে পারবে।
   *   *প্রশ্ন:* তুমি কি করতে পারো? (What are you allowed to do?)
   *   *পদ্ধতি:* Roles/Permissions (Admin, User, Manager).

---

#### **Lesson 7.2: JWT Basics (The VIP Pass)**

আগে আমরা কুকি (Session) ব্যবহার করতাম। কিন্তু API বা মোবাইল অ্যাপে কুকি কাজ করে না। তাই আমরা ব্যবহার করি **JWT (JSON Web Token)**।

**জিনিসটা কি?**
লগিন করার পর সার্ভার তোমাকে একটা লম্বা হিজিবিজি স্ট্রিং দেয় (Token)। এটা হলো তোমার **ডিজিটাল টিকেট**।
পরের বার রিকোয়েস্ট করার সময় হেডারে এই টিকেটটা দেখিয়ে বলবে, "বস, আমি রহিম, এই যে আমার টিকেট"। সার্ভার টিকেট চেক করে ঢুকতে দিবে।

**JWT এর ৩টা পার্ট:**
1.  **Header:** এনক্রিপশন অ্যালগরিদম কি?
2.  **Payload:** ডাটা (নাম, রোল, ইমেইল)। *সতর্কতা: এখানে পাসওয়ার্ড রাখবে না!*
3.  **Signature:** সিকিউরিটি সিল (যাতে কেউ টিকেট জাল করতে না পারে)।

---

#### **Lesson 7.3: Setting up ASP.NET Core Identity**

মাইক্রোসফট আমাদের জন্য পুরো ইউজার ম্যানেজমেন্ট সিস্টেম (টেবিল, পাসওয়ার্ড হ্যাশিং, লগিন লজিক) বানিয়ে রেখেছে। একে বলে **Identity**।

**Step 1: Install Packages**
*   `Microsoft.AspNetCore.Identity.EntityFrameworkCore`
*   `Microsoft.AspNetCore.Authentication.JwtBearer`

**Step 2: Custom User Model**
ডিফল্ট ইউজারের সাথে যদি এক্সট্রা ফিল্ড (যেমন FullName) চাও, তবে নতুন ক্লাস বানাও।

`Models/ApplicationUser.cs`:
```csharp
using Microsoft.AspNetCore.Identity;

public class ApplicationUser : IdentityUser
{
    public string FullName { get; set; } // এক্সট্রা ফিল্ড
}
```

**Step 3: Update DbContext**
`DbContext` এখন `IdentityDbContext` থেকে ইনহেরিট করবে।

`Data/AppDbContext.cs`:
```csharp
// আগে ছিল: public class AppDbContext : DbContext
public class AppDbContext : IdentityDbContext<ApplicationUser> 
{
    public AppDbContext(DbContextOptions<AppDbContext> options) : base(options)
    {
    }
}
```
*এখন `add-migration` আর `update-database` দিলে দেখবে ডাটাবেসে `AspNetUsers`, `AspNetRoles` সহ অনেকগুলো টেবিল তৈরি হয়ে গেছে! ম্যাজিক!* 🧙‍♂️

---

#### **Lesson 7.4: Register & Login Service**

আমরা সব লজিক একটা সার্ভিসে রাখবো `AuthService`।

**Constructor Injection:**
```csharp
public class AuthService : IAuthService
{
    private readonly UserManager<ApplicationUser> _userManager;
    private readonly SignInManager<ApplicationUser> _signInManager;
    private readonly IConfiguration _config;

    public AuthService(UserManager<ApplicationUser> userManager, IConfiguration config)
    {
        _userManager = userManager;
        _config = config;
    }
    // ... Methods
}
```

**Register Logic:**
```csharp
public async Task<string> Register(RegisterDto model)
{
    var user = new ApplicationUser 
    { 
        UserName = model.Email, 
        Email = model.Email, 
        FullName = model.FullName 
    };

    // পাসওয়ার্ড অটোমেটিক হ্যাশ হয়ে যাবে!
    var result = await _userManager.CreateAsync(user, model.Password);

    if (result.Succeeded) return "Success";
    return "Error";
}
```

---

#### **Lesson 7.5: Generating JWT Tokens (The Ticket Printer)**

লগিন সফল হলে আমরা টোকেন জেনারেট করবো।

```csharp
public string GenerateToken(ApplicationUser user, List<string> roles)
{
    var claims = new List<Claim>
    {
        new Claim(ClaimTypes.Name, user.UserName),
        new Claim(ClaimTypes.NameIdentifier, user.Id),
        new Claim(JwtRegisteredClaimNames.Jti, Guid.NewGuid().ToString())
    };

    // সব রোল টোকেনের ভেতর ঢুকিয়ে দাও
    foreach(var role in roles)
    {
        claims.Add(new Claim(ClaimTypes.Role, role));
    }

    var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_config["Jwt:Key"]));
    var creds = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);

    var token = new JwtSecurityToken(
        issuer: _config["Jwt:Issuer"],
        audience: _config["Jwt:Audience"],
        claims: claims,
        expires: DateTime.Now.AddHours(1), // ১ ঘণ্টা মেয়াদ
        signingCredentials: creds
    );

    return new JwtSecurityTokenHandler().WriteToken(token);
}
```
*Note: `appsettings.json`-এ `Jwt:Key` (একটা বিশাল লম্বা গোপন পাসওয়ার্ড) রাখতে হবে।*

---

#### **Lesson 7.6: Configuration in Program.cs**

এখন দারোয়ানকে গেটে বসাতে হবে।

```csharp
// 1. Identity সেটআপ
builder.Services.AddIdentity<ApplicationUser, IdentityRole>()
    .AddEntityFrameworkStores<AppDbContext>()
    .AddDefaultTokenProviders();

// 2. Authentication (JWT) সেটআপ
builder.Services.AddAuthentication(options =>
{
    options.DefaultAuthenticateScheme = JwtBearerDefaults.AuthenticationScheme;
    options.DefaultChallengeScheme = JwtBearerDefaults.AuthenticationScheme;
})
.AddJwtBearer(options =>
{
    options.TokenValidationParameters = new TokenValidationParameters
    {
        ValidateIssuer = true,
        ValidateAudience = true,
        ValidateLifetime = true,
        ValidateIssuerSigningKey = true,
        ValidIssuer = builder.Configuration["Jwt:Issuer"],
        ValidAudience = builder.Configuration["Jwt:Audience"],
        IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(builder.Configuration["Jwt:Key"]))
    };
});

// ...

var app = builder.Build();

// 3. Middleware Order (খুব জরুরি!)
app.UseAuthentication(); // আগে পরিচয় দাও
app.UseAuthorization();  // তারপর পারমিশন চেক
```

---

#### **Lesson 7.7: Authorize Attribute & Swagger Lock**

এখন আমরা কন্ট্রোলার প্রোটেক্ট করবো।

```csharp
[Authorize] // টোকেন ছাড়া কেউ ঢুকতে পারবে না
[HttpGet]
public IActionResult GetSecretData()
{
    return Ok("This is top secret!");
}
```

**Swagger-এ তালা (Lock Icon) আনার জন্য:**
`Program.cs` এ Swagger কনফিগারেশন একটু চেঞ্জ করতে হবে (SecurityDefinition যোগ করতে হবে)। এটা করলে Swagger-এই টোকেন পেস্ট করে টেস্ট করা যায়।

---

#### **Lesson 7.8: Role-Based Access Control (RBAC)**

সবাই সব কিছু করতে পারবে না।
*   **Admin:** সব ডিলিট করতে পারে।
*   **User:** শুধু দেখতে পারে।

**Controller:**
```csharp
[Authorize(Roles = "Admin")] // শুধু এডমিন ঢুকবে
[HttpDelete("{id}")]
public IActionResult DeleteStudent(int id) { ... }

[Authorize(Roles = "Admin,Teacher")] // এডমিন অথবা টিচার
[HttpPost]
public IActionResult UpdateMarks() { ... }
```

**Assign Role:**
রেজিস্ট্রেশনের সময় বা আলাদা API দিয়ে `_userManager.AddToRoleAsync(user, "Admin")` কল করে রোল দিতে হয়।

---

#### **Lesson 7.9: Claims-Based Authorization**

মাঝে মাঝে শুধু রোল দিয়ে হয় না। ধরো, যার বয়স ১৮+ শুধু সে ঢুকবে। এগুলোকে বলে **Policy**।

`Program.cs`:
```csharp
builder.Services.AddAuthorization(options =>
{
    options.AddPolicy("Over18", policy => policy.RequireClaim("AgeGroup", "Adult"));
});
```

`Controller`:
```csharp
[Authorize(Policy = "Over18")]
```

---

#### **Lesson 7.10: Module 7 Coding Exercise 🎯**

বড় এক্সারসাইজ বস! এটা করতে পারলে তুমি সাকসেসফুল ব্যাকএন্ড ডেভেলপার।

**Task: Build a Login System**

1.  `AuthController` বানাও।
2.  **POST /register**: ইউজারনেম, ইমেইল, পাসওয়ার্ড নিবে। ডাটাবেসে ইউজার ক্রিয়েট করবে।
3.  **POST /login**: ইমেইল আর পাসওয়ার্ড চেক করবে। ঠিক থাকলে একটা **JWT Token** রিটার্ন করবে।
4.  `StudentController`-এর মাথায় `[Authorize]` লাগাও।
5.  Swagger বা Postman দিয়ে টেস্ট করো:
    *   লগিন ছাড়া `/api/student` এক্সেস করার চেষ্টা করো -> `401 Unauthorized` আসবে।
    *   লগিন করে টোকেন নাও।
    *   টোকেনটা হেডার হিসেবে দিয়ে (Authorization: Bearer eyJhbG...) আবার ট্রাই করো -> `200 OK` আসবে।

---

**কি অবস্থা বস? মাথা ঘুরছে?** 😵‍💫
সিকিউরিটি একটু জটিল, কিন্তু একবার সেটআপ করে ফেললে আর টেনশন নাই।

**Summary:**
✅ Identity দিয়ে ইউজার ম্যানেজমেন্ট।
✅ JWT দিয়ে ডিজিটাল টিকেট বানানো।
✅ `[Authorize]` দিয়ে রাউট প্রটেক্ট করা।
✅ রোলের মাধ্যমে পাওয়ার কন্ট্রোল করা।

**Next Station: Module 8: Dependency Injection & Services** 💉
আমরা এতক্ষণ সব লজিক কন্ট্রোলারের পেটে লিখছিলাম। এটা বাজে প্র্যাকটিস। পরের মডিউলে আমরা কোড ক্লিন করবো এবং **Repository Pattern** শিখবো।

**এক্সারসাইজ শেষ করে কমেন্টে জানাও "I have the Power!", আমরা মডিউল ৮-এ মুভ করবো!** 💪🔐