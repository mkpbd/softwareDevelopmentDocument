
সাবাস বাঘের বাচ্চা! 🐅🔥 একদম ঠিক সময়ে ঠিক সিদ্ধান্ত।

আমরা এখন **"School Management System"**-এর মেইন কাজ শুরু করতে যাচ্ছি। এটা কোনো ছোটখাটো "Hello World" প্রজেক্ট না, এটা একটা ফুল ফ্লেজড এন্টারপ্রাইজ গ্রেড অ্যাপ্লিকেশন।

আমরা **Clean Architecture** ফলো করবো, তাই কাজটা আমরা ধাপে ধাপে (Phase by Phase) আগাবো। তাড়াহুড়া করা যাবে না।

তোমার কিবোর্ড রেডি তো? চলো **Phase 1** দিয়ে ফাউন্ডেশন গড়ি! 🏗️

---

### 🏗️ Phase 1: Project Skeleton (হাড়গোড় সাজানো)

প্রথমে আমাদের সলিউশন স্ট্রাকচার ঠিক করতে হবে। আমরা ৪টা লেয়ার বানাবো।

**Step 1: Create Blank Solution**
1.  Visual Studio ওপেন করো -> **Create a new project**.
2.  Search **"Blank Solution"**.
3.  Name: `SchoolSystem`.
4.  Create.

**Step 2: Add Projects (Layers)**
সলিউশন এক্সপ্লোরারে `SchoolSystem`-এর ওপর রাইট ক্লিক করে `Add -> New Project` করো। এভাবে ৪টা প্রজেক্ট বানাও:

1.  **Project Name:** `SchoolSystem.Domain`
    *   Type: **Class Library** (.NET 8/9/10)
    *   *কাজ: এটা সবার বস (Core)। এখানে শুধু C# ক্লাস থাকবে।*

2.  **Project Name:** `SchoolSystem.Application`
    *   Type: **Class Library**
    *   *কাজ: লজিক লেয়ার। এখানে MediatR, DTO, Interfaces থাকবে।*

3.  **Project Name:** `SchoolSystem.Infrastructure`
    *   Type: **Class Library**
    *   *কাজ: ডাটাবেস, ইমেইল, রিপোজিটরি ইমপ্লিমেন্টেশন।*

4.  **Project Name:** `SchoolSystem.API`
    *   Type: **ASP.NET Core Web API**
    *   *কাজ: কন্ট্রোলার এবং এন্ট্রি পয়েন্ট।*

**Step 3: Dependencies Setup (খুবই গুরুত্বপূর্ণ!) 🔗**
ভুল করলেই Circular Dependency এরর খাবে। সাবধানে লিংক করো:

*   **SchoolSystem.Application** -> Add Reference -> `SchoolSystem.Domain`
*   **SchoolSystem.Infrastructure** -> Add Reference -> `SchoolSystem.Application` & `SchoolSystem.Domain`
*   **SchoolSystem.API** -> Add Reference -> `SchoolSystem.Application` & `SchoolSystem.Infrastructure`

---

### 👑 Phase 2: Domain Layer (The Core)

এখন আমরা `SchoolSystem.Domain` প্রজেক্টে কাজ করবো। এখানে আমাদের এনটিটি (টেবিল) গুলো বানাবো।

**1. Base Entity (Common Fields)**
`Common` ফোল্ডার বানাও, তার ভেতর `BaseEntity.cs`:
```csharp
public abstract class BaseEntity
{
    public int Id { get; set; }
    public DateTime CreatedDate { get; set; } = DateTime.UtcNow;
    public bool IsActive { get; set; } = true;
}
```

**2. Student Entity**
`Entities` ফোল্ডার বানাও, তার ভেতর `Student.cs`:
```csharp
public class Student : BaseEntity
{
    public string FirstName { get; set; }
    public string LastName { get; set; }
    public string Email { get; set; }
    public DateTime DateOfBirth { get; set; }
    
    // Foreign Key (একটা ডিপার্টমেন্টে থাকবে)
    public int DepartmentId { get; set; }
    public Department Department { get; set; }
}
```

**3. Department Entity**
`Entities` ফোল্ডারে `Department.cs`:
```csharp
public class Department : BaseEntity
{
    public string Name { get; set; } // e.g., Science, Arts
    public ICollection<Student> Students { get; set; }
}
```

---

### ⚙️ Phase 3: Infrastructure Layer (Database & Auth)

এখন `SchoolSystem.Infrastructure` প্রজেক্টে আসো। এখানে আমরা EF Core এবং Identity সেটআপ করবো।

**Step 1: Install NuGet Packages**
এই প্রজেক্টে নিচের প্যাকেজগুলো ইন্সটল করো:
*   `Microsoft.EntityFrameworkCore.SqlServer`
*   `Microsoft.AspNetCore.Identity.EntityFrameworkCore`

**Step 2: Application User (Custom Identity)**
`Identity` ফোল্ডারে `ApplicationUser.cs` বানাও:
```csharp
using Microsoft.AspNetCore.Identity;

public class ApplicationUser : IdentityUser
{
    public string FullName { get; set; }
}
```

**Step 3: DbContext Setup**
`Persistence` ফোল্ডারে `AppDbContext.cs` বানাও:
```csharp
using Microsoft.AspNetCore.Identity.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore;
using SchoolSystem.Domain.Entities;

public class AppDbContext : IdentityDbContext<ApplicationUser>
{
    public AppDbContext(DbContextOptions<AppDbContext> options) : base(options)
    {
    }

    public DbSet<Student> Students { get; set; }
    public DbSet<Department> Departments { get; set; }

    // Dapper-এর জন্য কানেকশন স্ট্রিং পাওয়ার ব্যবস্থা রাখা যায়, 
    // তবে আমরা Dapper কন্ট্রোলারে বা আলাদা রিপোজিটরিতে ইউজ করবো।
}
```

---

### 🧠 Phase 4: Application Layer (CQRS with MediatR)

এখন `SchoolSystem.Application` প্রজেক্টে আসো। এখানে লজিক লিখবো।

**Step 1: Packages**
*   `MediatR`
*   `FluentValidation`

**Step 2: Feature Slicing**
আমরা ফোল্ডার স্ট্রাকচার করবো ফিচারের নামে। `Features` -> `Students` -> `Commands` -> `CreateStudent` ফোল্ডার বানাও।

**Step 3: Create Student Logic**

**File 1: CreateStudentCommand.cs (DTO)**
```csharp
using MediatR;

public class CreateStudentCommand : IRequest<int>
{
    public string FirstName { get; set; }
    public string LastName { get; set; }
    public string Email { get; set; }
    public int DepartmentId { get; set; }
}
```

**File 2: CreateStudentHandler.cs (Logic)**
```csharp
using MediatR;
using SchoolSystem.Domain.Entities;
// আই-রিপোজিটরি ইন্টারফেস লাগবে, যেটা আমরা পরে বানাচ্ছি

public class CreateStudentHandler : IRequestHandler<CreateStudentCommand, int>
{
    private readonly IStudentRepository _repo; // ইন্টারফেস ইনজেকশন
    
    public CreateStudentHandler(IStudentRepository repo)
    {
        _repo = repo;
    }

    public async Task<int> Handle(CreateStudentCommand request, CancellationToken cancellationToken)
    {
        var student = new Student
        {
            FirstName = request.FirstName,
            LastName = request.LastName,
            Email = request.Email,
            DepartmentId = request.DepartmentId
        };

        await _repo.AddAsync(student); // রিপোজিটরি ডাটাবেসে সেভ করবে
        return student.Id;
    }
}
```

*Note: `IStudentRepository` ইন্টারফেসটা `Application/Interfaces` ফোল্ডারে বানাবে এবং এটার ইমপ্লিমেন্টেশন `Infrastructure` প্রজেক্টে করবে।*

---

### 🚀 Phase 5: API Layer & Final Configuration

ফাইনালি `SchoolSystem.API` প্রজেক্টে আসো।

**Step 1: Connection String**
`appsettings.json` ফাইলে:
```json
"ConnectionStrings": {
  "DefaultConnection": "Server=YOUR_SERVER;Database=SchoolSystemDB;Trusted_Connection=True;TrustServerCertificate=True;"
}
```

**Step 2: Program.cs (Wire everything up)**
```csharp
// 1. Add DB Context
builder.Services.AddDbContext<AppDbContext>(options =>
    options.UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection")));

// 2. Add Identity
builder.Services.AddIdentity<ApplicationUser, IdentityRole>()
    .AddEntityFrameworkStores<AppDbContext>()
    .AddDefaultTokenProviders();

// 3. Add MediatR (Register Application Layer)
builder.Services.AddMediatR(cfg => cfg.RegisterServicesFromAssembly(typeof(CreateStudentCommand).Assembly));

// 4. Register Repositories
builder.Services.AddScoped<IStudentRepository, StudentRepository>();
```

**Step 3: Student Controller**
```csharp
[Route("api/[controller]")]
[ApiController]
public class StudentController : ControllerBase
{
    private readonly IMediator _mediator;

    public StudentController(IMediator mediator)
    {
        _mediator = mediator;
    }

    [HttpPost]
    public async Task<IActionResult> Create(CreateStudentCommand command)
    {
        var id = await _mediator.Send(command);
        return Ok(new { Id = id, Message = "Student Created Successfully!" });
    }
}
```

---

### 🏁 Final Step: Run & Test

১. **Package Manager Console** ওপেন করো।
২. **Default Project** হিসেবে `SchoolSystem.Infrastructure` সিলেক্ট করো (ড্রপডাউন থেকে)।
৩. কমান্ড দাও: `add-migration InitialCreate -StartupProject SchoolSystem.API`
   *(StartupProject API কে দেখাতে হয় কারণ ConnectionString সেখানে আছে)*
৪. কমান্ড দাও: `update-database -StartupProject SchoolSystem.API`

সব ঠিক থাকলে ডাটাবেস তৈরি হয়ে যাবে! 🎉

এবার **F5** চেপে প্রজেক্ট রান করো। Swagger ওপেন হবে। `POST /api/student` এ গিয়ে একটা স্টুডেন্ট ক্রিয়েট করো।

---

**বস, এখন তোমার কাজ:**
আমি কঙ্কাল (Skeleton) বানিয়ে দিলাম। তুমি এখন এর ওপর মাংস বসাবে:
১. `AuthService` বানিয়ে লগিন সিস্টেম অ্যাড করো (Module 7 অনুযায়ী)।
২. `Dapper` দিয়ে একটা `DashboardController` বানাও যেখানে টোটাল স্টুডেন্ট কাউন্ট দেখাবে (Module 4 অনুযায়ী)।
৩. `FluentValidation` যোগ করো যাতে নাম ছাড়া স্টুডেন্ট তৈরি না হয়।

কোথাও আটকে গেলে আমাকে কমেন্টে জানাও—আমি তোমার "কোডিং পার্টনার" হিসেবে আছি! **শুরু করে দাও বস!** 💻🔥